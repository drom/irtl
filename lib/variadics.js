'use strict';

const {
  add, sub,
  mul, div, rem,
  lt, leq, gt, geq, eq, neq,
  dshl, dshr,
  and, or, xor,
  cat,
  validif
} = require('./elements.js');

module.exports = {
  add, sub,
  mul, div, rem,
  lt, leq, gt, geq, eq, neq,
  dshl, dshr,
  and, or, xor,
  cat,
  validif
};
